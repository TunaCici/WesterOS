/*
 * Types.h
 *
 * Contains uint*_t typedefs for ARM64.
 */

#define NULL ((void*)0)

#define TRUE 1
#define FALSE 0

#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a,b) ((a) < (b) ? (a) : (b))
